# BIS Node.js EJS Port

This project is a Node.js + EJS scaffold for the Bacolod BIS CodeIgniter system.

## Setup

1. Install dependencies:

`ash
npm install
`

2. Run the app:

`ash
npm start
`

3. Open http://localhost:3000/secretary/dashboard

## Structure

- pp.js — Express app entrypoint
- outes/dashboard.js — route handler for role-based dashboard pages
- iews/ — EJS templates and partials
- public/css/style.css — shared UI styles
